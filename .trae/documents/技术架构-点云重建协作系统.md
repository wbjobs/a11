## 1. 架构设计

```mermaid
flowchart TB
    subgraph "客户端浏览器"
        subgraph "前端UI层 (React 18)"
            UI1["房间管理页面"]
            UI2["主工作区页面"]
            UI3["标注工具栏组件"]
            UI4["时间轴组件"]
            UI5["视频流面板组件"]
            UI6["侧边信息栏组件"]
        end
        
        subgraph "3D渲染层 (Three.js)"
            R1["PointCloudViewer 点云渲染器"]
            R2["AnnotationManager 标注管理器"]
            R3["SceneController 场景控制器"]
            R4["PostProcessing 后处理效果"]
        end
        
        subgraph "网络通信层"
            W1["WebSocket客户端"]
            Rtc1["WebRTC连接管理器"]
            H1["HTTP请求客户端"]
        end
        
        subgraph "数据缓存层"
            DB1["IndexedDB管理器"]
            S1["React Context状态管理"]
            S2["Zustand全局状态"]
        end
    end
    
    subgraph "服务端 (Python FastAPI)"
        API1["FastAPI HTTP接口"]
        WS1["WebSocket连接管理"]
        RM1["房间管理器"]
        AM1["标注同步器"]
        
        subgraph "重建引擎"
            C1["COLMAP包装器"]
            C2["视频帧队列"]
            C3["特征提取模块"]
            C4["稀疏重建模块"]
            C5["点云数据序列化"]
        end
    end
    
    UI1 --> H1
    UI2 --> R1
    UI3 --> R2
    UI4 --> DB1
    UI5 --> Rtc1
    UI6 --> S2
    
    R1 --> R3
    R2 --> R3
    R3 --> R4
    
    W1 --> WS1
    Rtc1 --> Rtc1
    H1 --> API1
    
    S1 --> UI1
    S2 --> UI2
    DB1 --> UI4
    
    API1 --> C2
    WS1 --> RM1
    RM1 --> AM1
    AM1 --> WS1
    
    C2 --> C3
    C3 --> C4
    C4 --> C5
    C5 --> WS1
```

## 2. 技术描述

- **前端**：React 18 + TypeScript + Vite + TailwindCSS 3 + Zustand
- **3D渲染**：Three.js + @react-three/fiber + @react-three/drei + @react-three/postprocessing
- **WebRTC**：simple-peer 库封装
- **WebSocket**：原生WebSocket + 重连机制
- **本地存储**：IndexedDB (idb库封装)
- **后端**：Python 3.10 + FastAPI + Uvicorn + WebSockets
- **三维重建**：COLMAP (通过subprocess调用) + OpenCV
- **异步任务**：asyncio + Queue

## 3. 路由定义

| 路由 | 页面组件 | 用途 |
|------|----------|------|
| / | RoomPage | 首页，创建/加入房间 |
| /room/:roomId | WorkspacePage | 主工作区，点云渲染和标注 |

## 4. API定义

### 4.1 HTTP接口

```typescript
// 创建房间
POST /api/room/create
Request: { username: string }
Response: { roomId: string, userId: string, token: string }

// 加入房间
POST /api/room/join
Request: { roomId: string, username: string, role: 'creator' | 'collaborator' | 'viewer' }
Response: { userId: string, token: string, users: User[], pointCloud?: PointCloudData }

// 上传视频帧
POST /api/frame/upload
Headers: { Authorization: Bearer {token}, Room-Id: {roomId} }
Request: multipart/form-data { frame: File, timestamp: number, frameIndex: number }
Response: { success: boolean, queued: boolean }

// 获取点云历史列表
GET /api/pointcloud/history?roomId={roomId}
Response: { versions: PointCloudVersion[] }

// 获取指定版本点云
GET /api/pointcloud/:versionId
Response: { points: number[], colors: number[], timestamp: number }
```

### 4.2 WebSocket消息类型

```typescript
type WebSocketMessage =
  | { type: 'user_join'; data: User }
  | { type: 'user_leave'; data: { userId: string } }
  | { type: 'pointcloud_update'; data: PointCloudData }
  | { type: 'annotation_add'; data: Annotation }
  | { type: 'annotation_update'; data: Annotation }
  | { type: 'annotation_delete'; data: { annotationId: string } }
  | { type: 'reconstruct_status'; data: { status: 'idle' | 'processing' | 'done', progress: number } }
  | { type: 'webrtc_offer'; data: { from: string, to: string, offer: RTCSessionDescriptionInit } }
  | { type: 'webrtc_answer'; data: { from: string, to: string, answer: RTCSessionDescriptionInit } }
  | { type: 'webrtc_ice'; data: { from: string, to: string, candidate: RTCIceCandidateInit } }
```

## 5. 服务端架构图

```mermaid
flowchart LR
    subgraph "接口层"
        HTTP["FastAPI HTTP路由"]
        WS["WebSocket连接"]
    end
    
    subgraph "服务层"
        RM["房间管理服务"]
        RS["重建服务"]
        AS["标注同步服务"]
        UM["用户管理服务"]
    end
    
    subgraph "核心引擎"
        FQ["帧队列管理器"]
        FE["COLMAP特征提取"]
        SR["稀疏重建器"]
        PC["点云生成器"]
    end
    
    subgraph "数据层"
        Mem["内存存储"]
        Disk["磁盘缓存"]
    end
    
    HTTP --> RM
    HTTP --> UM
    WS --> AS
    
    RM --> RS
    UM --> Mem
    
    AS --> Mem
    RS --> FQ
    FQ --> FE
    FE --> SR
    SR --> PC
    PC --> WS
    
    FQ --> Disk
    PC --> Disk
```

## 6. 数据模型

### 6.1 数据模型定义

```mermaid
erDiagram
    ROOM ||--o{ USER : has
    ROOM ||--o{ POINTCLOUD_VERSION : has
    ROOM ||--o{ ANNOTATION : has
    USER ||--o{ ANNOTATION : creates
    POINTCLOUD_VERSION ||--o{ ANNOTATION : "annotated on"
    
    ROOM {
        string id PK
        string name
        string creatorId FK
        datetime createdAt
        string status
    }
    
    USER {
        string id PK
        string roomId FK
        string name
        string role
        string socketId
        datetime joinedAt
    }
    
    POINTCLOUD_VERSION {
        string id PK
        string roomId FK
        int versionNumber
        int pointCount
        float progress
        datetime createdAt
        string dataPath
    }
    
    ANNOTATION {
        string id PK
        string roomId FK
        string userId FK
        string pointCloudVersionId FK
        string type
        float positionX
        float positionY
        float positionZ
        float directionX
        float directionY
        float directionZ
        string color
        float size
        string text
        datetime createdAt
        datetime updatedAt
    }
```

### 6.2 IndexedDB数据结构

```typescript
// IndexedDB Store 定义
const dbSchema = {
  roomInfo: { key: 'roomId', indexes: ['createdAt'] },
  pointCloudVersions: { key: 'versionId', indexes: ['roomId', 'timestamp', 'versionNumber'] },
  pointCloudData: { key: 'versionId', indexes: ['roomId'] },
  annotations: { key: 'annotationId', indexes: ['roomId', 'versionId', 'userId', 'createdAt'] },
  localFrames: { key: 'frameId', indexes: ['roomId', 'timestamp'] }
};

// 点云数据结构
interface PointCloudData {
  versionId: string;
  roomId: string;
  timestamp: number;
  versionNumber: number;
  points: Float32Array;  // [x, y, z, x, y, z, ...]
  colors: Float32Array;  // [r, g, b, r, g, b, ...]
  pointCount: number;
  cameraPoses?: CameraPose[];
}

// 标注数据结构
interface Annotation {
  annotationId: string;
  roomId: string;
  userId: string;
  userName: string;
  pointCloudVersionId: string;
  type: 'arrow' | 'sphere' | 'text';
  position: [number, number, number];
  direction?: [number, number, number];  // for arrow
  color: string;
  size: number;
  text?: string;
  createdAt: number;
  updatedAt: number;
}

// 用户数据结构
interface User {
  userId: string;
  roomId: string;
  name: string;
  role: 'creator' | 'collaborator' | 'viewer';
  isOnline: boolean;
  hasVideo: boolean;
  videoStreamId?: string;
}
```
